# Equestria at War: With Fire and Sword *(indev)*


## Comment tags in files
+ `(WFAS [PCWM] +)` – WFAS addition to an EAW file
(possible `[PCWM]` stands for *PCW Module*)
+ `(WFAS [PCWM] ~)` – WFAS change to an EAW file
+ `(!)` – error
+ `(*)` – other note
